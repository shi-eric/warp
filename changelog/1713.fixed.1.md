Fix launches doing nothing, without reporting anything, after a module failed to build in the native compiler. Once
a build failed, every later launch of any kernel in that module returned as though the kernel had run, including
kernels unrelated to the failure. The build error is now reported on those launches.
