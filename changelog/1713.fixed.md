Fix a kernel that fails to compile reporting the error only on its first launch. Later launches on the same device
silently ran a partially built kernel that skipped the failing statement and everything after it, and launches on
other devices raised an unrelated `KeyError`. The compilation error is now raised on every launch and every device,
while other kernels in the same module continue to build.
